<p>Leaders: <strong><%= (@widget_leaders.count > 0) ? @widget_leaders.map{|u| link_to_user(u)}.join(', ') : '<em class="alert">No leaders!</em>' %></strong></p>
<h4>Assignee Pool</h4>
<p>Find out more about the assignees on the <%= link_to('community page for this widget',people_community_url(@widget.community.id)) %></p>
<% if @widget_assignees.length > 0 %>
  <p><strong class="groupicon"><%= @widget_assignees.size %> active user<%= @widget_assignees.size > 1 ? 's' : '' %></strong> <%= @widget_assignees.size > 1 ? 'are' : 'is' %> signed up to answer questions from this widget.</p>
  <ul id="assignee_pool">
  <% @widget_assignees.each do |user| %>
    <li><%= link_to(user.fullname, aae_profile_url(:id => user.login), {:class => "profile"}) %></li>
  <% end %>
  </ul>
<% else %>
    <div class="askalert">
        <p><strong class="groupicon">There are no active users signed up to receive questions from this widget.</strong></p>
    </div>
<% end %>