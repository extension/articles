<%= error_messages_for 'sponsor' %>

<!--[form:sponsor]-->
<p>
<%= label :sponsor, :logo_id, "Which graphic will this ad use?" %>
	<%= select :sponsor, :logo_id, Logo.find(:all).collect { |a| [a.filename, a.id] }.delete_if { |a| a[0].include? "_thumb." } %>
</p>
<p>
	<span>"main" displays ad only on the eXtension homepage. Other options display ad on the homepage and any page that includes the content tag.</span>
</p>
<!--[eoform:sponsor]-->

