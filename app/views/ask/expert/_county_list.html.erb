<% if location and location.counties.length > 0 %>
	<label>
	County (optional)
	<%= select_tag ('expert_question[county_id]', options_for_select([["", ""]] + location.counties.map{|c| [c.name, c.id]}, @expert_question ? @expert_question.county_id : nil ), :disabled => disabled) %>
    </label>
<% end %>
