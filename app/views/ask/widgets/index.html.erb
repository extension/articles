<% @selected_tab = :all %>

<h2>Ask an Expert Widgets</h2>

<ul id="nav">
    <li><%= link_to "Active", :controller => :widgets, :action => :index %></li>
    <li><%= link_to "In-Active", :controller => :widgets, :action => :index, :id => 'inactive' %></li>
</ul>

<br class="clear"/>

<p>
    <%= label_tag 'widget_name', 'By Name (start typing to filter widgets)' %>
    <%= text_field_tag('widget_name') %>
</p>

<%= observe_field(:widget_name,
                  :url       => { :controller => :widgets, :action => :get_widgets, :id => params[:id] },
                  :update    => :widget_list,
                  :frequency => 0.6,
                  :with      => "'widget_name=' + value")
%>

<div id="widget_list">
<%= render(:partial => 'widget_list') %>
</div>