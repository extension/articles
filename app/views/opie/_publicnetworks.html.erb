<% showuser.social_networks.each do |social_network| %>
<% if social_network.is_public? %>
<dl class="usernetwork">
  <dt class="<%= social_network.network %>"><%= social_network_name(social_network) %></dt>
  <dd><%= social_network_link(social_network) %></dd>
  </dl>
<% end %>
<% end %>