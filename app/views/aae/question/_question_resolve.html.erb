<%= render(:partial => "aae/search/answer_search") %>

<div class="aae_module">
    <h3>Answer or Ask For More Info</h3>
    <p class="button2">
        <%= link_to 'Reply', answer_question_url(:squid => @submitted_question.id) %>
    </p>
    
    <p class="clear">If this question is outside of eXtension's expertise, please use this sample text to suggest their local office: <%= link_to 'Please contact your local office...', answer_question_url(:squid => @submitted_question.id, :sample => SubmittedQuestion::DECLINE_ANSWER, :status_state => SubmittedQuestion::STATUS_NO_ANSWER) %></p>
</div>


<h5>Moderate this Question</h5>
<p><%= link_to 'Reject', {:controller => 'aae/question', :action => :reject, :id => @submitted_question.id} %> (inappropriate or duplicate questions)</p>

<% if !@submitted_question.asked_question.index("http://").nil? %>
<p><%= link_to "Report as Spam", aae_report_spam_url(:id => @submitted_question.id), 
                               :confirm => "Are you sure you want to report this question as spam?", :method => :post %></p>
<% end %>