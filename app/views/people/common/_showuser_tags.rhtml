<div class="tag_cloud">
    <ul> 
        <% showuser.tags.each do |tag| %>
          <li><%= link_to(tag.name,:controller=> :colleagues, :action => :tags, :taglist=>tag.name) %></li>
        <% end %>
    </ul>
</div>
