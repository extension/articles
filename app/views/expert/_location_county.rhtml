
	<%= select_tag ('expert_question[location_id]', options_for_select([["", ""]] + @locations.map{|l| [l.name, l.id]}, @expert_question.location_id ), :disabled => disabled) %>
	<%= observe_field('expert_question_location_id',
	                  :update    => :county_list,
	                  :url       => { :action => :get_counties },
	                  :with      => "'location_id='+value") %>

<p id="county_list">
<%= render(:partial => 'county_list', :locals => {:location=> @expert_question.location, :disabled => disabled} ) %>
</p>